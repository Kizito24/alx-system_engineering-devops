#!/usr/bin/env bash
apt update
apt install haproxy
cp -a /etc/haproxy/haproxy.cfg{,.orig}
config="backend mybackend\n\tbalance roundrobin\n\tserver serv1 \t100.25.203.147 check\n\tserver serv2 \t100.25.104.117 check"
echo "$config" >> /etc/haproxy/haproxy.cfg
service haproxy restart
